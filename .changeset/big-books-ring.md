---
'mastracode-remote': patch
---

Fixed bare mastra workflow commands so the TUI immediately asks whether to run the existing plan or plan a new feature before making a model request. Use mastra workflow --run to execute an existing plan with unlimited goal persistence.
