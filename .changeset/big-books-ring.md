---
'mastracode-remote': patch
---

Fixed bare mastra workflow commands so the TUI asks whether to run the existing plan or plan a new feature before making a model request. Both choices use unlimited goal persistence; the new-feature path completes the interview and planning stages before implementation.
