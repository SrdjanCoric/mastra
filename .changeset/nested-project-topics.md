---
'mastracode-remote': patch
---

Keep the current working directory as the Telegram project when it is nested inside a parent Git repository, so each nested app gets its own correctly named topic.
